<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorDarkMaterialBackground"
    tools:context=".fragments.PublishFragment"
    android:clickable="true">

    <!-- TODO: Update blank fragment layout -->
    <com.projekt.tdp028.views.FlexButton
        android:id="@+id/flex_publish_button"
        android:layout_height="100dp"
        android:layout_width="100dp"
        android:backgroundTint="@color/colorDarkMaterialOnBackground"
        android:text="@string/make_poll_title"
        />

    <Button
        android:id="@+id/publish_poll_button"
        android:layout_width="269dp"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/colorAccent"
        android:text="@string/publish_poll"
        android:textColor="#FFFFFF"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>